package model;

// This class represents the Administrator of the system.
// It doesn't have much, because the only relevant fields are the static username and password, defined below.
public class Admin {

    // Login credentials are static for user of the program
    public static final String username = "admin";
    public static final String password = "1234";

    // constructor method for Admin class
    public Admin() {
    }
}
